# BERT fraudulent job detection — dependencies
# Prefer the existing E: CUDA environment rather than creating a new CPU env.
#
# Activate:
#   . E:\ml\activate.ps1
#
# Or invoke:
#   E:\path\to\cuda_environment\python.exe -m pip install -r requirements.txt

torch>=2.0.0
transformers>=4.36.0
accelerate>=0.25.0
scikit-learn>=1.3.0
pandas>=2.0.0
numpy>=1.24.0
matplotlib>=3.7.0
seaborn>=0.13.0
tqdm>=4.65.0
safetensors>=0.4.0
